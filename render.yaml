services:
  - type: web
    name: bangalore-home-prices
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT Server.server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
